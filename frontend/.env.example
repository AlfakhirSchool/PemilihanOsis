# Backend tidak punya prefix /api (routes di root: /auth, /election, /admin) — pakai subdomain terpisah,
# bukan path di bawah voting.smpialfakhir.sch.id, karena /admin dipakai juga oleh halaman frontend /admin/*
NEXT_PUBLIC_API_URL=https://api-voting.smpialfakhir.sch.id
